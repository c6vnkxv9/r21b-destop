export interface IRoleOptions {
    value: string;
    text: string;
}

export interface IScript {
    key: string,
    name: string,
    label: string,
    route: string,
    required: string[],
    options: string[]
}

